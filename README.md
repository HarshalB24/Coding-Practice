# Coding-Practice
This will have all the practice problems for Python, SQL, Spark.

#whenever you sync git repo to local , always follow below steps :
1.download git to your local system
2.install it
3.clone your repo in VS Code
4.copy the HTTPS link for your repo and write command "git clone "your repo link"
5.once the repo is cloned -> come to the directory in which your repo is synced
6.after making any changes :
    1.git status
    2.git add .
    3.git commit -m "type your message here"
    4.git push
